spring:
  application:
    name: im-server
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
  config:
    import: nacos:im-server.yml


server:
  port: 9000  # 网关端口

#spring:
#  application:
#    name: api-gateway
#
#  cloud:
#    nacos:
#      discovery:
#        server-addr: ${NACOS_SERVER:localhost:8848}
#
#    gateway:
#      # 全局跨域配置
#      globalcors:
#        cors-configurations:
#          '[/**]':
#            allowedOrigins: "*"
#            allowedMethods:
#              - GET
#              - POST
#              - PUT
#              - DELETE
#              - OPTIONS
#            allowedHeaders: "*"
#            allowCredentials: true
#            maxAge: 3600
#
#      # 路由配置
#      routes:
#        # WebSocket 路由 - 方式1：直接地址
#        - id: chat-websocket-direct
#          uri: ws://localhost:8080  # 你的 IM 服务地址
#          predicates:
#            - Path=/ws/**
#          filters:
#            - StripPrefix=1  # 去掉 /ws 前缀
#
#        # WebSocket 路由 - 方式2：服务发现（推荐）
#        - id: chat-websocket-service
#          uri: lb:ws://im-service  # lb:ws 表示负载均衡的 WebSocket
#          predicates:
#            - Path=/ws/**
#          filters:
#            - StripPrefix=1
#
#        # HTTP API 路由（如果有）
#        - id: chat-api
#          uri: lb:http://im-service
#          predicates:
#            - Path=/api/**
#          filters:
#            - StripPrefix=1
#
#        # 健康检查路由
#        - id: chat-health
#          uri: lb:http://im-service
#          predicates:
#            - Path=/actuator/health
#
#      # 可选：默认过滤器
#      default-filters:
#        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin
#
#      # 超时配置
#      httpclient:
#        connect-timeout: 10000
#        response-timeout: 180s  # WebSocket 长连接需要较长超时
#
#      # WebSocket 配置
#      websocket:
#        proxy:
#          enabled: true
#
## 可选：日志配置
#logging:
#  level:
#    org.springframework.cloud.gateway: DEBUG
#    reactor.netty: DEBUG